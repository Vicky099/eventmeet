<%= render "shared/mailer/heading", title: "Event approved", subtitle: @event.name %>

<div style="color:#686868;padding-top:16px;font-size:13px;line-height:1.6;">
  Your agency reviewed <strong style="color:#1f1f1f;"><%= @event.name %></strong> and approved it — you can now continue setting it up.
</div>

<%= render "shared/mailer/button", url: admin_event_url(@event), label: "Continue Setup →" %>
