<% content_for :page_title, "New Account" %>

<%= render "shared/page_header",
  icon: "bx-buildings",
  crumbs: [ { label: "Tenants", path: platform_accounts_path }, { label: "New Account" } ] %>

<div class="row">
  <div class="col-xl-7 col-lg-9">
    <div class="card">
      <div class="card-body">
        <%= render "form", account: @account %>
      </div>
    </div>
  </div>
</div>
