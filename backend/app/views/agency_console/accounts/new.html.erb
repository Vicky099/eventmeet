<% content_for :page_title, "New Tenant" %>

<%= render "shared/page_header",
  icon: "bx-buildings",
  crumbs: [ { label: Current.agency.name, path: agency_root_path }, { label: "New Tenant" } ] %>

<div class="row">
  <div class="col-xl-7 col-lg-9">
    <div class="card">
      <div class="card-body">
        <%= render "form", account: @account %>
      </div>
    </div>
  </div>
</div>
